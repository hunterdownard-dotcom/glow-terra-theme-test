<style>
  :root{
    --gt-cart-ink:#211a16;
    --gt-cart-muted:#675c52;
    --gt-cart-soft:#fbf8f1;
    --gt-cart-card:#fffdf8;
    --gt-cart-line:#e4d4c3;
    --gt-cart-green:#344b3a;
    --gt-cart-green-2:#4f6447;
    --gt-cart-gold:#b18a55;
  }

  body:has(.cart-page),
  body:has(cart-drawer),
  body:has(.cart-drawer){
    background:var(--gt-cart-soft);
  }

  .cart-page,
  cart-items-component .section,
  cart-items-component .section-background{
    background:radial-gradient(circle at 10% 0%,rgba(177,138,85,.14),transparent 30%),linear-gradient(180deg,#fbf8f1 0%,#fffdf8 58%,#eef1e8 100%)!important;
    color:var(--gt-cart-ink);
  }

  .cart-page{
    padding-block:34px 64px;
  }

  .cart-page__title,
  .cart-page__items,
  .cart-page__summary,
  .cart-page__more-blocks{
    color:var(--gt-cart-ink);
  }

  .cart-title,
  .cart-page h1,
  .cart-page h2,
  .cart-page h3,
  .cart-page .h1,
  .cart-page .h2,
  .cart-page .h3{
    color:var(--gt-cart-ink);
    letter-spacing:0;
  }

  .cart-page__items,
  .cart-page__summary > *,
  .cart-summary,
  .cart__summary,
  .cart-page .cart-items,
  .cart-page .cart-items__table,
  .cart-page .cart-item,
  .cart-page .product-card,
  .cart-page .product-card-wrapper{
    border-color:rgba(52,75,58,.13)!important;
  }

  .cart-page__summary,
  .cart-summary,
  .cart__summary,
  .cart-page .cart-items,
  .cart-page .cart-items__table,
  .cart-page .cart-item,
  .cart-page .product-card,
  .cart-page .product-card-wrapper{
    background:linear-gradient(180deg,#fffdf8,#f8f0e5)!important;
    border-radius:8px!important;
    box-shadow:0 20px 58px rgba(52,75,58,.1);
  }

  .cart-page .cart-item,
  .cart-page .cart-items tr,
  .cart-page .cart-items__row{
    border-bottom:1px solid var(--gt-cart-line)!important;
  }

  .cart-page img,
  cart-drawer img,
  .cart-drawer img{
    border-radius:8px;
    background:#efe4d7;
  }

  .cart-page a,
  cart-drawer a,
  .cart-drawer a{
    color:var(--gt-cart-ink);
    text-decoration-color:rgba(52,75,58,.28);
  }

  .cart-page .price,
  .cart-page .totals,
  .cart-page .cart-item__price-wrapper,
  cart-drawer .price,
  cart-drawer .totals,
  .cart-drawer .price,
  .cart-drawer .totals{
    color:var(--gt-cart-ink);
  }

  .cart-page .caption,
  .cart-page .caption-large,
  .cart-page .product-option,
  .cart-page small,
  cart-drawer .caption,
  cart-drawer .caption-large,
  cart-drawer .product-option,
  cart-drawer small,
  .cart-drawer .caption,
  .cart-drawer .caption-large,
  .cart-drawer .product-option,
  .cart-drawer small{
    color:var(--gt-cart-muted);
  }

  .cart-page button[name="checkout"],
  .cart-page .cart__checkout-button,
  .cart-page .button,
  cart-drawer button[name="checkout"],
  cart-drawer .cart__checkout-button,
  cart-drawer .button,
  .cart-drawer button[name="checkout"],
  .cart-drawer .cart__checkout-button,
  .cart-drawer .button{
    background:linear-gradient(135deg,var(--gt-cart-green),var(--gt-cart-green-2))!important;
    border:1px solid rgba(52,75,58,.24)!important;
    border-radius:999px!important;
    color:#fff!important;
    box-shadow:0 18px 40px rgba(52,75,58,.22);
    font-weight:800;
  }

  .cart-page input,
  .cart-page textarea,
  .cart-page select,
  cart-drawer input,
  cart-drawer textarea,
  cart-drawer select,
  .cart-drawer input,
  .cart-drawer textarea,
  .cart-drawer select{
    background:#fffdf8!important;
    border:1px solid var(--gt-cart-line)!important;
    border-radius:8px!important;
    color:var(--gt-cart-ink)!important;
  }

  cart-drawer .drawer__inner,
  .cart-drawer .drawer__inner,
  #CartDrawer,
  .cart-drawer{
    background:var(--gt-cart-soft)!important;
    color:var(--gt-cart-ink)!important;
  }

  cart-drawer .drawer__header,
  .cart-drawer .drawer__header,
  cart-drawer .drawer__footer,
  .cart-drawer .drawer__footer,
  cart-drawer .cart-drawer__footer,
  .cart-drawer .cart-drawer__footer{
    background:linear-gradient(180deg,#fffdf8,#f8f0e5)!important;
    border-color:var(--gt-cart-line)!important;
    color:var(--gt-cart-ink)!important;
  }

  cart-drawer .cart-item,
  .cart-drawer .cart-item,
  cart-drawer .drawer__cart-items-wrapper,
  .cart-drawer .drawer__cart-items-wrapper{
    background:#fffdf8!important;
    border-color:rgba(52,75,58,.13)!important;
  }

  cart-drawer .drawer__heading,
  .cart-drawer .drawer__heading{
    color:var(--gt-cart-ink)!important;
  }

  @media(max-width:749px){
    .cart-page{
      padding-block:22px 44px;
    }
  }
</style>
