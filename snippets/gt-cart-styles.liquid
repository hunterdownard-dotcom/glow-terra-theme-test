<style>
  :root{
    --gt-cart-ink:#211a16;
    --gt-cart-muted:#675c52;
    --gt-cart-soft:#fbf8f1;
    --gt-cart-card:#fffdf8;
    --gt-cart-line:#e4d4c3;
    --gt-cart-green:#344b3a;
    --gt-cart-green-2:#4f6447;
    --gt-cart-gold:#b18a55;
  }

  @media(min-width:990px){
    #header-group .header-section,
    #header-component{
      background:rgba(64,81,63,.82)!important;
      backdrop-filter:blur(10px);
      -webkit-backdrop-filter:blur(10px);
      overflow:hidden!important;
      box-shadow:none!important;
    }

    #header-component a,
    #header-component button,
    #header-component .header-actions__action{
      transition:color .18s ease,opacity .18s ease!important;
    }

    #header-component a:hover,
    #header-component a:focus,
    #header-component a:focus-visible,
    #header-component button:hover,
    #header-component button:focus,
    #header-component button:focus-visible,
    #header-component .header-actions__action:hover,
    #header-component .header-actions__action:focus,
    #header-component .header-actions__action:focus-visible{
      background:transparent!important;
      box-shadow:none!important;
      transform:none!important;
      margin:0!important;
      outline:0!important;
    }

    #header-component .header-actions__action:has(.header-actions__cart-icon),
    #header-component .header-actions__action:has(.cart-icon-bubble),
    #header-component .header-actions__action:has(.header__icon--cart),
    #header-component .header-actions__action:has(a[href="/cart"]),
    #header-component .header-actions__action:has(a[href*="/cart"]),
    #header-component .header-actions__cart,
    #header-component .action__cart,
    #header-component a[href="/cart"],
    #header-component a[href*="/cart"],
    #header-component .cart-icon-bubble,
    #header-component .header__icon--cart,
    #header-component .header-actions__cart-icon{
      display:none!important;
    }
  }

  @media(max-width:749px){
    #header-group,
    #header-group .header-section,
    #header-component{
      display:none!important;
    }
  }

  search-modal,
  .search-modal,
  predictive-search,
  .predictive-search{
    --gt-search-green:#40513f;
    --gt-search-cream:#fffdf8;
    --gt-search-line:#d9cabc;
  }

  search-modal input[type="search"]:focus,
  search-modal input[type="text"]:focus,
  .search-modal input[type="search"]:focus,
  .search-modal input[type="text"]:focus,
  predictive-search input[type="search"]:focus,
  predictive-search input[type="text"]:focus,
  .predictive-search input[type="search"]:focus,
  .predictive-search input[type="text"]:focus{
    border-color:var(--gt-search-green)!important;
    box-shadow:0 0 0 2px rgba(64,81,63,.14)!important;
    outline:0!important;
  }

  search-modal button[aria-label*="close" i],
  .search-modal button[aria-label*="close" i],
  predictive-search button[aria-label*="close" i],
  .predictive-search button[aria-label*="close" i]{
    width:42px!important;
    height:42px!important;
    min-width:42px!important;
    min-height:42px!important;
    display:inline-flex!important;
    align-items:center!important;
    justify-content:center!important;
    background:var(--gt-search-green)!important;
    border:0!important;
    border-radius:50%!important;
    color:var(--gt-search-cream)!important;
    box-shadow:0 14px 34px rgba(33,26,22,.18)!important;
    line-height:1!important;
    padding:0!important;
  }

  search-modal button[aria-label*="close" i] svg,
  .search-modal button[aria-label*="close" i] svg,
  predictive-search button[aria-label*="close"
